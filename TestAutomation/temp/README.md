This directory is used for storing the test output.
However, as Git does not like empty directories, this file is 
designed to keep the directory without maintaining an out.html 
file in the directory.